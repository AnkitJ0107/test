Git Commands

1. To check git version
   git --version

2. To initialize new Git Repository
   git init

3. To add files in staging area
    git add filename
4. To commit files on repository
    git commit -m "initilal commit"
5. To push git repository in remote origin
   git push -u origin main
